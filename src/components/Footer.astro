<footer class="footer">
  <div class="footer-content">
    <div class="privacy-notice">
      <h3>We value your privacy</h3>
      <p>We use cookies to enhance your browsing experience and analyze our traffic. By clicking "Accept All", you consent to our use of cookies.</p>
      <p>Blockchain4Indonesia © 2024</p>
    </div>
    
    <div class="footer-buttons">
      <button class="footer-button accept">Accept All</button>
      <button class="footer-button reject">Reject All</button>
    </div>
  </div>
</footer>

<style>
  .footer {
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(10px);
    padding: 2rem max(5vw, 1rem);
    margin-top: auto;
  }

  .footer-content {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 2rem;
    align-items: center;
  }

  .privacy-notice h3 {
    margin: 0 0 1rem;
    color: white;
  }

  .privacy-notice p {
    margin: 0.5rem 0;
    color: rgba(255, 255, 255, 0.8);
  }

  .footer-buttons {
    display: flex;
    gap: 1rem;
  }

  .footer-button {
    padding: 0.75rem 1.5rem;
    border-radius: 2rem;
    border: 2px solid;
    background: transparent;
    font-weight: 500;
    cursor: pointer;
    transition: var(--transition);
  }

  .footer-button.accept {
    border-color: var(--text-color);
    color: var(--text-color);
  }

  .footer-button.reject {
    border-color: var(--primary-color);
    color: var(--primary-color);
  }

  .footer-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
  }

  .footer-button.accept:hover {
    background: var(--text-color);
    color: white;
  }

  .footer-button.reject:hover {
    background: var(--primary-color);
    color: white;
  }

  @media (max-width: 768px) {
    .footer-content {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .footer-buttons {
      justify-content: center;
      flex-wrap: wrap;
    }
  }
</style>
<script></script>